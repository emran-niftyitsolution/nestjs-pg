# internal
NODE_ENV=development
PORT=5001

# database
# Neon pooled connection string (PgBouncer, -pooler host) — see README for the direct/unpooled variant.
DATABASE_URL=postgresql://user:password@host-pooler.region.aws.neon.tech/dbname?sslmode=require
